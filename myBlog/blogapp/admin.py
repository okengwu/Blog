from django.contrib import admin
from .models import Post  # import the table to work on the admin 

admin.site.register(Post) # this helps to register to help you work from the dashboard

# Register your models here.
